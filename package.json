{
  "name": "tanks-game",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "server": "node server/server.js",
    "start": "npm run server",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
